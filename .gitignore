# OS
.DS_Store
Thumbs.db

# IDEs
.vscode/
.idea/
*.iml

# Java / Maven (Frontend)
frontend/target/
frontend/dependency-reduced-pom.xml
*.class
*.log

# Go (Backend)
backend/backend.exe
backend/main.exe
backend/backend
# Ignore compiled binaries
*.exe
*.dll
*.so
*.dylib
# Go modules
vendor/

# Installer
installer_input/
installer_output/

# Local Development Config
backend/config.json

# Note: backend/notes/ seems to contain sample data, so we keep it.
# But we should ignore the .git folder if the app initializes one there during dev
backend/notes/.git/
